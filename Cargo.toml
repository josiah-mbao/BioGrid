[package]
name = "biogrid"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.13"
rand = "0.8"
noise = "0.9"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
approx = "0.5"
